# 🔄 System Flow Diagrams

Visual representation of the complete Weelo Logistics system flow.

---

## 📊 Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         WEELO LOGISTICS SYSTEM                      │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   CUSTOMER   │         │ TRANSPORTER  │         │    DRIVER    │
│   (Android)  │         │   (Android)  │         │   (Android)  │
└──────┬───────┘         └──────┬───────┘         └──────┬───────┘
       │                        │                        │
       │                        │                        │
       └────────────────────────┼────────────────────────┘
                                │
                    ┌───────────▼───────────┐
                    │   BACKEND SERVER      │
                    │   - REST APIs         │
                    │   - WebSocket Server  │
                    │   - Push Notifications│
                    └───────────┬───────────┘
                                │
                    ┌───────────┴───────────┐
                    │                       │
            ┌───────▼───────┐       ┌──────▼──────┐
            │   DATABASE    │       │   REDIS     │
            │   (MySQL)     │       │   (Cache)   │
            └───────────────┘       └─────────────┘
```

---

## 🚀 Flow 1: Broadcast Creation & Assignment

```
┌─────────────┐
│  CUSTOMER   │ "I need 10 container trucks"
└──────┬──────┘
       │
       │ 1. POST /broadcasts
       │    {
       │      pickupLocation: {...},
       │      dropLocation: {...},
       │      totalTrucksNeeded: 10,
       │      vehicleType: "CONTAINER"
       │    }
       ▼
┌──────────────────────────────────┐
│  BACKEND: Create Broadcast       │
│  ────────────────────────────    │
│  1. Validate request             │
│  2. Calculate fare (algorithm)   │
│  3. Create broadcast record      │
│  4. Find nearby transporters     │
│     (50km radius, has vehicles)  │
│  5. Send notifications           │
│     - Push (FCM)                 │
│     - WebSocket                  │
└──────────┬───────────────────────┘
           │
           │ Push Notification
           ├────────────────────────────┐
           │                            │
           ▼                            ▼
    ┌─────────────┐            ┌─────────────┐
    │TRANSPORTER A│            │TRANSPORTER B│
    │"I can take  │            │"I can take  │
    │ 3 trucks"   │            │ 4 trucks"   │
    └──────┬──────┘            └──────┬──────┘
           │                          │
           │ 2. POST /assignments     │
           │    {                     │
           │      broadcastId: "...", │
           │      trucksTaken: 3,     │
           │      assignments: [      │
           │        {driverId, vehicleId},
           │        {driverId, vehicleId},
           │        {driverId, vehicleId}
           │      ]                   │
           │    }                     │
           ▼                          ▼
    ┌──────────────────────────────────────┐
    │  BACKEND: Create Assignment          │
    │  ──────────────────────────────       │
    │  1. Validate availability            │
    │  2. Update broadcast.trucks_filled   │
    │  3. Create assignment record         │
    │  4. Create driver notifications      │
    │  5. Send to drivers                  │
    │     - Push (HIGH PRIORITY)           │
    │     - WebSocket                      │
    │     - Full-screen alarm trigger      │
    └──────────┬───────────────────────────┘
               │
               │ CRITICAL: Full-screen notification
               │
               ├─────────┬─────────┐
               ▼         ▼         ▼
           ┌───────┐ ┌───────┐ ┌───────┐
           │DRIVER1│ │DRIVER2│ │DRIVER3│
           │       │ │       │ │       │
           │🔔🔊📳 │ │🔔🔊📳 │ │🔔🔊📳 │
           │ALARM! │ │ALARM! │ │ALARM! │
           └───┬───┘ └───┬───┘ └───┬───┘
               │         │         │
               ▼         ▼         ▼
           ACCEPT    ACCEPT    DECLINE
```

---

## 🎯 Flow 2: Driver Notification & Response

```
┌────────────────────────────────────────┐
│  TRANSPORTER ASSIGNS DRIVER TO TRIP   │
└────────────────┬───────────────────────┘
                 │
                 │ POST /assignments
                 ▼
┌──────────────────────────────────────────────┐
│  BACKEND: Send Driver Notification           │
│  ──────────────────────────────────           │
│  1. Create notification record               │
│  2. Get driver's FCM token                   │
│  3. Send FCM (HIGH priority)                 │
│     {                                        │
│       notification: {                        │
│         title: "🚛 New Trip Assignment!",    │
│         body: "Pickup → Drop | ₹2,500",      │
│         sound: "trip_alarm.mp3"              │
│       },                                     │
│       data: {                                │
│         type: "TRIP_ASSIGNMENT",             │
│         fullScreenAlarm: "true",             │
│         tripData: {...}                      │
│       }                                      │
│     }                                        │
│  4. Send WebSocket (if online)               │
│  5. Schedule auto-decline (5 min)            │
└──────────────────┬───────────────────────────┘
                   │
                   │ Notification delivered in < 2 seconds
                   ▼
         ┌─────────────────────┐
         │   DRIVER'S PHONE    │
         │  ──────────────────  │
         │  🔔 FULL SCREEN     │
         │  🔊 LOUD ALARM      │
         │  📳 VIBRATION       │
         │                     │
         │  ┌───────────────┐  │
         │  │ NEW TRIP!     │  │
         │  │               │  │
         │  │ From: CP      │  │
         │  │ To: Noida     │  │
         │  │ Fare: ₹2,500  │  │
         │  │ Dist: 32.5 km │  │
         │  │               │  │
         │  │ ┌───────────┐ │  │
         │  │ │  ACCEPT   │ │  │
         │  │ └───────────┘ │  │
         │  │ ┌───────────┐ │  │
         │  │ │  DECLINE  │ │  │
         │  │ └───────────┘ │  │
         │  └───────────────┘  │
         │                     │
         │  Expires in 4:59    │
         └──────┬──────────────┘
                │
                │ Driver Decision
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
    ACCEPT          DECLINE
        │               │
        │               │ POST /notifications/:id/decline
        │               │ {reason: "Vehicle issue"}
        │               ▼
        │           ┌────────────────────────┐
        │           │ BACKEND: Handle Decline│
        │           │ 1. Update status       │
        │           │ 2. Notify transporter  │
        │           │ 3. Create reassignment │
        │           └────────────────────────┘
        │
        │ POST /notifications/:id/accept
        ▼
┌────────────────────────────────────┐
│  BACKEND: Driver Accepted          │
│  ────────────────────────────      │
│  1. Update notification status     │
│  2. Update assignment status       │
│  3. Create trip record             │
│  4. Initialize GPS tracking        │
│  5. Notify transporter (accepted)  │
│  6. Notify customer (driver info)  │
└────────────┬───────────────────────┘
             │
             │ Response includes:
             │ - Trip details
             │ - Tracking ID
             │ - WebSocket URL
             ▼
      GPS TRACKING STARTS
```

---

## 📍 Flow 3: GPS Tracking & Live Location

```
┌────────────────────────────┐
│  DRIVER ACCEPTED TRIP      │
└────────────┬───────────────┘
             │
             │ POST /tracking/initialize
             ▼
┌────────────────────────────────────────┐
│  BACKEND: Initialize GPS Tracking      │
│  ────────────────────────────────       │
│  1. Create tracking record             │
│  2. Generate tracking_id               │
│  3. Return WebSocket URL               │
│  4. Set update interval (10 seconds)   │
└────────────┬───────────────────────────┘
             │
             │ trackingId: "TRACK-001-XYZ"
             │ websocketUrl: "wss://..."
             ▼
┌────────────────────────────────────────┐
│  DRIVER APP: Start Location Service   │
│  ────────────────────────────────      │
│  1. Request location permissions       │
│  2. Start background service           │
│  3. Get GPS coordinates every 10s      │
│  4. Send to backend                    │
└────────────┬───────────────────────────┘
             │
             │ Every 10 seconds
             │
       ┌─────┴─────┐
       │           │
       ▼           ▼
   T = 0s      T = 10s
   POST /tracking/:id/location
   {
     latitude: 28.6500,
     longitude: 77.2300,
     speed: 0,
     heading: 0
   }
       │
       ▼
┌────────────────────────────────────────┐
│  BACKEND: Process Location Update      │
│  ────────────────────────────────       │
│  1. Validate tracking session          │
│  2. Store in location_history table    │
│  3. Update current_location            │
│  4. Calculate metrics                  │
│     - Distance traveled                │
│     - Distance to pickup/drop          │
│     - ETA calculation                  │
│  5. Broadcast via WebSocket            │
│     to customer & transporter          │
└────────────┬───────────────────────────┘
             │
             │ WebSocket Broadcast
             │
       ┌─────┴─────┐
       │           │
       ▼           ▼
┌──────────┐  ┌────────────┐
│CUSTOMER  │  │TRANSPORTER │
│          │  │            │
│ [MAP]    │  │ [FLEET]    │
│   📍     │  │   📍       │
│  Driver  │  │  Vehicle   │
│          │  │            │
│ ETA: 18m │  │ Speed: 45  │
└──────────┘  └────────────┘
```

---

## 🎬 Flow 4: Trip Progress States

```
┌──────────────┐
│ TRIP CREATED │ ← Driver accepts
└──────┬───────┘
       │ GPS enabled
       │
       ▼
┌─────────────────────────────┐
│ WAITING_TO_START            │ ← Location updates begin
│ - Driver moving to pickup   │
│ - Customer sees live location│
└──────┬──────────────────────┘
       │
       │ POST /tracking/:id/pickup-reached
       │
       ▼
┌─────────────────────────────┐
│ AT_PICKUP                   │
│ - Notify customer           │
│ - Driver loading goods      │
└──────┬──────────────────────┘
       │
       │ POST /tracking/:id/start-trip
       │
       ▼
┌─────────────────────────────┐
│ IN_TRANSIT                  │
│ - Location shared live      │
│ - Customer tracks on map    │
│ - ETA calculated            │
└──────┬──────────────────────┘
       │
       │ POST /tracking/:id/drop-reached
       │
       ▼
┌─────────────────────────────┐
│ AT_DROP                     │
│ - Notify customer           │
│ - Driver unloading          │
└──────┬──────────────────────┘
       │
       │ POST /tracking/:id/complete
       │ {otp: "1234", signature: "..."}
       │
       ▼
┌─────────────────────────────┐
│ COMPLETED                   │
│ - Tracking stopped          │
│ - Calculate earnings        │
│ - Update statistics         │
└─────────────────────────────┘
```

---

## 🔄 Flow 5: Real-time Communication (WebSocket)

```
┌──────────────────────────────────────────────────┐
│           WebSocket Server Architecture          │
└──────────────────────────────────────────────────┘

CLIENT CONNECTION
─────────────────
┌──────────┐
│  Client  │
│  Apps    │
└────┬─────┘
     │
     │ ws://api.weelologistics.com/ws
     │ + JWT token
     ▼
┌────────────────────────────┐
│  Socket.io Server          │
│  ──────────────────         │
│  1. Validate JWT           │
│  2. Subscribe to rooms:    │
│     - user:{userId}        │
│     - role:{role}          │
│     - transporter:{id}     │
│     - customer:{id}        │
└────────┬───────────────────┘
         │
         │ Connection established
         │
         ▼

ROOM SUBSCRIPTIONS
──────────────────
┌─────────────────────────────────────────────┐
│  Rooms (Channels)                           │
│  ───────────────                            │
│                                             │
│  role:TRANSPORTER  ← All transporters       │
│  role:DRIVER       ← All drivers            │
│  role:CUSTOMER     ← All customers          │
│                                             │
│  user:DRV-001      ← Specific driver        │
│  user:TRANS-567    ← Specific transporter   │
│  user:CUST-123     ← Specific customer      │
│                                             │
│  transporter:TRANS-567 ← Transporter's data │
│  customer:CUST-123     ← Customer's data    │
│                                             │
│  tracking:TRACK-001    ← Specific trip      │
└─────────────────────────────────────────────┘

EVENT FLOW
──────────
Backend Server           WebSocket Server         Clients
──────────────           ────────────────         ───────
      │                         │                    │
      │ emit('broadcast:new')   │                    │
      ├────────────────────────►│                    │
      │                         │                    │
      │                         │ To: role:TRANSPORTER
      │                         ├───────────────────►│
      │                         │                    │ Transporter 1
      │                         ├───────────────────►│ Transporter 2
      │                         │                    │ Transporter 3
      │                         │                    │
      │                         │                    │
      │ emit('trip:assigned',   │                    │
      │      to: 'DRV-001')     │                    │
      ├────────────────────────►│                    │
      │                         │                    │
      │                         │ To: user:DRV-001   │
      │                         ├───────────────────►│ Driver 1 only
      │                         │                    │
      │                         │                    │
      │ emit('location:update', │                    │
      │      to: 'TRACK-001')   │                    │
      ├────────────────────────►│                    │
      │                         │                    │
      │                         │ To: tracking:TRACK-001
      │                         ├───────────────────►│ Customer
      │                         ├───────────────────►│ Transporter
      │                         │                    │
```

---

## 🔔 Flow 6: Push Notification Architecture

```
┌────────────────────────────────────────────────┐
│       Firebase Cloud Messaging (FCM)           │
└────────────────────────────────────────────────┘

NOTIFICATION TRIGGER
────────────────────
Backend Event
(e.g., Driver assigned)
      │
      ▼
┌─────────────────────────────┐
│  Backend Server             │
│  ───────────────            │
│  1. Get driver's FCM token  │
│  2. Prepare payload         │
│  3. Send via Firebase SDK   │
└────────┬────────────────────┘
         │
         │ Firebase Admin SDK
         ▼
┌────────────────────────────┐
│  Firebase Cloud Messaging  │
│  (Google's servers)        │
└────────┬───────────────────┘
         │
         │ Push to device
         │
         ▼
┌─────────────────────────────┐
│  Driver's Android Device    │
│  ─────────────────────────  │
│  1. FCM Service receives    │
│  2. App triggers alarm      │
│  3. Show full-screen UI     │
└─────────────────────────────┘

NOTIFICATION TYPES
──────────────────
┌──────────────────────────────────────────┐
│ TYPE: TRIP_ASSIGNMENT (CRITICAL)         │
│ Priority: HIGH                           │
│ Sound: trip_alarm.mp3                    │
│ Behavior: Full-screen alarm              │
│ Timeout: 5 minutes                       │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ TYPE: DRIVER_ACCEPTED (INFO)             │
│ Priority: DEFAULT                        │
│ Sound: default                           │
│ Behavior: Normal notification            │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ TYPE: TRIP_STARTED (INFO)                │
│ Priority: DEFAULT                        │
│ Sound: default                           │
│ Behavior: Normal notification            │
└──────────────────────────────────────────┘
```

---

## ⚡ Flow 7: Race Condition Handling

```
PROBLEM: Multiple transporters assign simultaneously
─────────────────────────────────────────────────────

Broadcast: 10 trucks needed
Transporter A: Assigns 7 trucks at T=0
Transporter B: Assigns 5 trucks at T=0.1s
Total: 12 trucks (EXCEEDS LIMIT!)

SOLUTION: Database Transaction with Lock
─────────────────────────────────────────

┌────────────────────────────────────────┐
│  Request A arrives (7 trucks)          │
└────────┬───────────────────────────────┘
         │
         ▼
START TRANSACTION
         │
         ▼
SELECT * FROM broadcasts 
WHERE broadcast_id = ? 
FOR UPDATE  ← LOCKS THE ROW
         │
         ▼
Check: (trucks_filled + 7) <= total_trucks_needed
       (0 + 7) <= 10 ✓ OK
         │
         ▼
UPDATE broadcasts
SET trucks_filled = trucks_filled + 7
WHERE broadcast_id = ?
         │
         ▼
COMMIT TRANSACTION  ← RELEASES LOCK
         │
         │
         │ Meanwhile, Request B is waiting...
         │
         ▼
┌────────────────────────────────────────┐
│  Request B arrives (5 trucks)          │
└────────┬───────────────────────────────┘
         │
         ▼
START TRANSACTION
         │
         ▼
SELECT * FROM broadcasts 
WHERE broadcast_id = ? 
FOR UPDATE  ← WAITS FOR LOCK
         │
         ▼ (Lock released, now proceeds)
Check: (trucks_filled + 5) <= total_trucks_needed
       (7 + 5) <= 10 ✗ FAILS!
         │
         ▼
ROLLBACK TRANSACTION
         │
         ▼
Return Error: "Only 3 trucks remaining"
```

---

## 📊 Database Relationships Diagram

```
┌───────────────┐
│  customers    │
│───────────────│
│ customer_id PK│◄─────────┐
│ name          │          │
│ mobile        │          │
└───────────────┘          │
                           │
                    ┌──────┴──────────┐
                    │  broadcasts     │
                    │─────────────────│
                    │ broadcast_id PK │◄────────┐
                    │ customer_id FK  │         │
                    │ pickup_lat      │         │
                    │ drop_lat        │         │
                    │ trucks_needed   │         │
                    │ trucks_filled   │         │
                    └─────────────────┘         │
                           │                    │
                    ┌──────┴────────────┐       │
                    │ trip_assignments  │       │
                    │───────────────────│       │
                    │ assignment_id PK  │◄──────┼────┐
                    │ broadcast_id FK   │       │    │
                    │ transporter_id FK │       │    │
                    │ trucks_taken      │       │    │
                    └───────────────────┘       │    │
                           │                    │    │
      ┌────────────────────┴─────┐              │    │
      │                          │              │    │
      ▼                          ▼              │    │
┌──────────────────┐   ┌────────────────────┐  │    │
│driver_truck_     │   │driver_notifications│  │    │
│assignments       │   │────────────────────│  │    │
│──────────────────│   │notification_id PK  │  │    │
│assignment_id FK  │   │assignment_id FK    │  │    │
│driver_id FK      │   │driver_id FK        │  │    │
│vehicle_id FK     │   │status              │  │    │
│status            │   └────────────────────┘  │    │
└──────────────────┘                           │    │
      │                                         │    │
      │ (on ACCEPT)                             │    │
      ▼                                         │    │
┌───────────────┐                               │    │
│  trips        │                               │    │
│───────────────│                               │    │
│ trip_id PK    │───────────────────────────────┘    │
│ assignment_id │                                    │
│ broadcast_id  │────────────────────────────────────┘
│ driver_id FK  │
│ vehicle_id FK │
│ status        │
└───────┬───────┘
        │
        ▼
┌────────────────────┐
│live_trip_tracking  │
│────────────────────│
│tracking_id PK      │
│trip_id FK          │
│current_lat         │
│current_lng         │
│last_updated        │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│location_history    │
│────────────────────│
│id PK               │
│tracking_id FK      │
│latitude            │
│longitude           │
│timestamp           │
└────────────────────┘
```

---

## 🎯 Critical Success Metrics

```
┌──────────────────────────────────────────┐
│  Performance KPIs                        │
├──────────────────────────────────────────┤
│  API Response Time      < 200ms     ✓    │
│  Notification Delivery  < 2 sec     ✓    │
│  WebSocket Latency     < 100ms     ✓    │
│  GPS Update Interval    10 sec      ✓    │
│  Database Query        < 50ms      ✓    │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  Reliability KPIs                        │
├──────────────────────────────────────────┤
│  Notification Success   99.9%      ✓    │
│  API Uptime            99.99%     ✓    │
│  GPS Accuracy          ±10m       ✓    │
│  Zero data loss         100%       ✓    │
└──────────────────────────────────────────┘
```

---

**End of System Flow Diagrams**

Refer to individual API documentation for detailed implementation specs.
